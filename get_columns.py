import subprocess
import json

# Assuming mysql is available and user is root or similar. But wait, I can just parse my previous `tampil()` Object array!
# Wait, in tampil(), we added `this.rs.getString("nyeri_waktu_cerna")`? No!
# Let's extract the exact columns used in simpan()!
# Wait, in `tampil()`, we saw `this.rs.getString(...)` up to `cpot_kategori`.
